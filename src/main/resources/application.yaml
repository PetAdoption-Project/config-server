server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI}
          search-paths: config
          default-label: ${CONFIG_REPO_BRANCH:main}
